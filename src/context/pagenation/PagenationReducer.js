const pagenationReducer = (state, action) => {
  const { type, payload } = action;
  switch (type) {
    // case "SINGLE_MEAL_PAGENATION":
    //   return {
    //     ...state,

    //   }
    default:
      break;
  }
};

export default pagenationReducer;
